// A versão do sistema que a tela mostra (R229, U119). É a MESMA do package.json —
// o verificador compara as duas — e é a que o pacote Windows grava em VERSAO.txt.
// Davi, 08/09/2026: "qualquer alteração que façamos será executada via
// versionamento do sistema". O histórico do que entrou em cada versão está em
// docs/VERSOES.md.
export const VERSAO = "0.0.5";
